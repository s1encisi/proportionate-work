# Proportionate Work

供支持 Skill 的智能体使用的规则包：按实际任务风险安排工作与验证，减少无关操作，同时保留必要检查。

## 使用

`SKILL.md` 为中文入口，`SKILL.en.md` 为英文入口；`references/` 为按需加载的核心规则，`adapters/` 为精简接入规则，`examples/` 为验收场景。

将规则包放入宿主支持的 Skill 目录并显式调用。具体加载方式取决于所用宿主。本仓库不自动修改宿主配置。

许可证：[MIT](LICENSE)。
